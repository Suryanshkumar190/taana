# Copy this file to .env and fill in your key for local testing with
# `vercel dev`. On Vercel itself, set this in Project Settings →
# Environment Variables — never commit your real key.
#
# Get a free Groq API key (no credit card needed) at:
# https://console.groq.com -> API Keys -> Create API Key
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxx
